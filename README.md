Hay dos modos en el dashboard:

Modo Demo: No recibe datos, no hay grafica solo hay una plantilla diseñada. El modo demo es cuando no se activa la api solo lo en el archivo html oprimes lives y se despliega el dashboard modo demo

Modo en Vivo: Se activa cuando encendemos la api primero en la terminal con el comando npm run dev y luego en el archivo html oprimes lives y ya se activaria el modo en vivo. El modo en vivo recibe datos de la api, el cual hace una grafica funcional y dinamico al igual que los datos de la tabla
